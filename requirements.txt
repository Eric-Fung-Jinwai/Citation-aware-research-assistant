# API & Web
fastapi==0.136.1
uvicorn==0.47.0
httpx==0.28.1
requests==2.34.2
python-dotenv==1.2.2
pydantic==2.13.4

# LLM
openai==2.36.0

# RAG utilities (text splitting for SEC filing chunking)
langchain-text-splitters==1.1.2

# Market Data (Polygon.io accessed directly via httpx; calendars for trading-day math)
pandas-market-calendars==5.3.2

# Technical Indicators
# pandas pinned <3: streamlit 1.38 requires pandas<3, pandas-ta requires >=2.3.2
pandas==2.3.3
pandas-ta==0.4.71b0

# Vector DB & RAG
chromadb==1.5.9
sentence-transformers==5.5.1
# torch >=2.4 required by transformers 5.x; macOS wheels are arm64-only (no Intel after 2.2.2)
torch==2.12.0

# Frontend (Phase 5 — not yet installed in env)
streamlit==1.38.0

# Caching
redis[asyncio]==6.2.0

# Testing (not yet installed in env)
pytest==8.3.3
pytest-asyncio==0.24.0